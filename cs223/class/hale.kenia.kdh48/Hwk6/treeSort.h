#ifndef _NUM_H
#define _NUM_H

#include <stdio.h>

/*
 * High-precision arithmetic on non-negative number in base 10.
 */

typedef struct tree Tree;
typedef struct node Node;


/*Tree * treeMakeRandom(int lo, int hi);

int treeSize(Tree *t);

int treeHeight(Tree *t);*/

#endif /* _NUM_H */
